import { Footer, Header, ProfileSection } from "../components";

const HomePage = () => {
    return (
        <div>
            <Header />
            <div>HomePage</div>

            <ProfileSection />

            <Footer />
        </div>
    );
};

export default HomePage;
